# CHECKLIST.md - Чеклист перед развертыванием

## Подготовка проекта

### Локально (на вашей машине)
- [ ] Протестировали бота локально (`python main.py`)
- [ ] Создали Git репозиторий (`git init`)
- [ ] Добавили все файлы в git (`git add .`)
- [ ] Сделали первый коммит (`git commit -m "Initial commit"`)
- [ ] Создали репозиторий на GitHub/GitLab
- [ ] Загрузили код в облако (`git push`)
- [ ] Проверили что `.env` находится в `.gitignore`
- [ ] Убедились что в git НЕ загружен `.env` файл
- [ ] Создали `.env.example` с пустыми значениями

### Сервер и хостинг
- [ ] Выбрали хостинга (VPS, DigitalOcean, Hetzner и т.д.)
- [ ] Получили IP сервера
- [ ] Получили SSH доступ (root пароль или SSH ключ)
- [ ] Сервер на Ubuntu 20.04 LTS или Debian 11+

### Telegram Bot
- [ ] Создали бота через @BotFather
- [ ] Получили BOT_TOKEN
- [ ] Запомнили свой Telegram ID (для ADMIN_ID)
- [ ] Создали/получили ID групп для уведомлений
- [ ] Добавили бота в 6 групп:
  - [ ] Общая группа (general)
  - [ ] Группа Айгерим
  - [ ] Группа Бермет
  - [ ] Группа Майрам
  - [ ] Группа Жайна
  - [ ] Группа Чолпон
- [ ] Дали боту права администратора в группах
- [ ] Скопировали все ID групп

---

## Развертывание на сервере

### Предварительная настройка сервера
- [ ] Подключились по SSH (`ssh root@server_ip`)
- [ ] Обновили систему (`apt update && apt upgrade -y`)
- [ ] Установили Python 3.9
- [ ] Установили Git
- [ ] Создали директорию `/opt/mama_reg_bots`
- [ ] Перешли в директорию `cd /opt/mama_reg_bots`

### Клонирование и установка
- [ ] Клонировали репозиторий (`git clone ...`)
- [ ] Создали виртуальное окружение (`python3.9 -m venv venv`)
- [ ] Активировали окружение (`source venv/bin/activate`)
- [ ] Установили зависимости (`pip install -r requirements.txt`)
- [ ] Скопировали .env.example (`cp .env.example .env`)

### Конфигурация
- [ ] Отредактировали `.env` файл (`nano .env`)
- [ ] Добавили BOT_TOKEN
- [ ] Добавили ADMIN_ID
- [ ] Добавили все ID групп (6 групп)
- [ ] Сохранили файл (Ctrl+X, Y, Enter)

### Подготовка папок
- [ ] Создали папки для кураторов:
  ```bash
  mkdir -p data/Айгерим data/Бермет data/Майрам data/Жайна data/Чолпон
  ```

### Запуск сервиса
- [ ] Создали `/etc/systemd/system/mama-bot.service`
- [ ] Добавили нужное содержимое в service файл
- [ ] Перезагрузили systemd (`sudo systemctl daemon-reload`)
- [ ] Включили автозапуск (`sudo systemctl enable mama-bot`)
- [ ] Запустили сервис (`sudo systemctl start mama-bot`)

### Проверка
- [ ] Проверили статус (`sudo systemctl status mama-bot`)
- [ ] Посмотрели логи (`sudo journalctl -u mama-bot -f`)
- [ ] Нет ошибок в логах
- [ ] Бот отвечает на сообщения в Telegram
- [ ] Фото загружаются в группы
- [ ] Данные сохраняются в папки

---

## Безопасность

- [ ] `.env` файл НЕ в git репозитории
- [ ] `.env` находится в `.gitignore`
- [ ] `.env` на сервере имеет правильные права (700)
- [ ] Папка `data/` имеет правильные права (755)
- [ ] Регулярно делаете резервные копии
- [ ] SSH ключи безопасны (если используете)
- [ ] Файрволл настроен на сервере (если нужно)

### Дополнительная безопасность
- [ ] Создали резервную копию на другой сервер
- [ ] Протестировали восстановление из резервной копии
- [ ] Включили автоматические резервные копии (cron)
- [ ] Настроили мониторинг свободного места на диске

---

## Обслуживание

### Мониторинг
- [ ] Проверяете логи регулярно
- [ ] Следите за свободным местом на диске
- [ ] Проверяете что бот запущен (`systemctl status`)

### Обновления
- [ ] Когда нужно обновить код - используете `git pull origin main`
- [ ] После обновления перезагружаете сервис
- [ ] Сначала создаете резервную копию
- [ ] Проверяете логи после обновления

### Резервные копии
- [ ] Созданы вручную 1 раз
- [ ] Автоматические копии настроены на cron
- [ ] Проверили что копии сохраняются
- [ ] Знаете как восстановиться из резервной копии

---

## Документация

- [ ] Скопировали `README.md` на сервер
- [ ] Скопировали `DEPLOYMENT.md` на сервер
- [ ] Скопировали `QUICK_START.md` на сервер
- [ ] Знаете где найти команды для управления ботом

---

## Финальная проверка

- [ ] ✅ Бот запущен на сервере
- [ ] ✅ Бот отвечает на `/start`
- [ ] ✅ Пользователи могут зарегистрироваться
- [ ] ✅ Фото загружаются корректно
- [ ] ✅ Данные сохраняются в Excel
- [ ] ✅ Уведомления приходят в группы
- [ ] ✅ Логи чистые (без ошибок)
- [ ] ✅ Резервные копии работают
- [ ] ✅ Вы знаете как обновить код
- [ ] ✅ Вы знаете как решить проблемы

---

## Полезные команды

```bash
# Управление ботом
sudo systemctl status mama-bot      # Статус
sudo systemctl restart mama-bot     # Перезагрузить
sudo systemctl stop mama-bot        # Остановить
sudo systemctl start mama-bot       # Запустить

# Логи
sudo journalctl -u mama-bot -f      # Real-time
sudo journalctl -u mama-bot -n 50   # Последние 50 строк

# Обновление
cd /opt/mama_reg_bots
git pull origin main
sudo systemctl restart mama-bot

# Резервная копия
tar -czf backup.tar.gz data/ config/

# Проверка диска
df -h
du -sh /opt/mama_reg_bots/data/
```

---

## Если возникают проблемы

1. **Бот не запускается**
   - Проверьте логи: `sudo journalctl -u mama-bot -n 50`
   - Проверьте .env файл
   - Проверьте права доступа

2. **Фото не загружаются**
   - Проверьте свободное место: `df -h`
   - Проверьте права на папку data
   - Проверьте интернет соединение

3. **Ошибка "Token invalid"**
   - Проверьте BOT_TOKEN в .env
   - Убедитесь что токен скопирован правильно
   - Получите новый токен у @BotFather

4. **Высокая нагрузка на CPU/RAM**
   - Возможно бот накопил логи
   - Очистите старые логи: `journalctl --vacuum=time:7d`
   - Очистите старые резервные копии

---

## Контрольная сумма

**Минимум файлов которые должны быть на сервере:**
- `main.py` ✅
- `requirements.txt` ✅
- `.env` ✅ (с вашими значениями)
- `config/` папка ✅
- `handlers/` папка ✅
- `utils/` папка ✅
- `data/` папка с подпапками кураторов ✅

**Файлы которые НЕ должны быть в git:**
- `.env` ❌
- `__pycache__/` ❌
- `venv/` ❌
- `data/` (пользовательские данные) ❌
- `*.xlsx` (может быть) ❌

---

## Успешно развернули! 🎉

Если всё прошло успешно - поздравляем! Ваш бот готов к использованию.

Не забывайте:
- 📊 Мониторить логи
- 💾 Делать резервные копии
- 🔄 Обновлять код регулярно
- 📈 Проверять статистику регистраций

Удачи! 🚀
